/** @type {import('eslint/lib/shared/types').ConfigData} */
const config = {
  root: true,

  extends: ['@marbemac/eslint-config/react', 'plugin:@next/next/recommended'],
};

module.exports = config;
